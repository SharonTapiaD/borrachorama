CREATE TABLE proveedores (
    id INT AUTO_INCREMENT PRIMARY KEY,
    nombre VARCHAR(150) NOT NULL,
    contacto VARCHAR(150),
    telefono VARCHAR(20),
    correo VARCHAR(150),
    direccion TEXT,
    estatus ENUM('activo','inactivo') DEFAULT 'activo'
);

CREATE TABLE pedidos_proveedor (
    id INT AUTO_INCREMENT PRIMARY KEY,
    proveedor_id INT NOT NULL,
    fecha_pedido DATE NOT NULL,
    fecha_entrega_estimada DATE,
    estado ENUM('pendiente','enviado','recibido','cancelado') DEFAULT 'pendiente',
    total DECIMAL(10,2),
    FOREIGN KEY (proveedor_id) REFERENCES proveedores(id)
);

CREATE TABLE detalle_pedido_proveedor (
    id INT AUTO_INCREMENT PRIMARY KEY,
    pedido_id INT NOT NULL,
    producto_id INT NOT NULL,
    cantidad INT NOT NULL,
    precio_unitario DECIMAL(10,2),
    FOREIGN KEY (pedido_id) REFERENCES pedidos_proveedor(id),
    FOREIGN KEY (producto_id) REFERENCES productos(id)
);

CREATE TABLE inventario (
    id INT AUTO_INCREMENT PRIMARY KEY,
    producto_id INT NOT NULL,
    stock_actual INT DEFAULT 0,
    stock_minimo INT DEFAULT 10,
    FOREIGN KEY (producto_id) REFERENCES productos(id)
);

CREATE TABLE movimientos_logisticos (
    id INT AUTO_INCREMENT PRIMARY KEY,
    producto_id INT NOT NULL,
    tipo ENUM('entrada','salida','ajuste'),
    cantidad INT NOT NULL,
    referencia VARCHAR(150),
    fecha TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (producto_id) REFERENCES productos(id)
);

ALTER TABLE pedidos_proveedor
ADD COLUMN producto_id INT(11) NOT NULL AFTER proveedor_id,
ADD COLUMN tipo ENUM('reposicion', 'venta') NOT NULL DEFAULT 'reposicion' AFTER estado;

ALTER TABLE pedidos_proveedor
ADD CONSTRAINT fk_pedido_producto 
FOREIGN KEY (producto_id) REFERENCES productos(id);

INSERT INTO clientes (nombre, correo, fecha_registro, estado, etapa_crm, usuario_id)
SELECT nombre, correo, CURDATE(), 'activo', 'Prospecto', id
FROM usuarios
WHERE correo = 'danna@gmail.com';